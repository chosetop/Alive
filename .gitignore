.worktrees/

# Root Docker Compose secrets. Keep only the safe template in Git.
.env
.env.*
!.env.docker.example

# Local task/runtime logs.
.superpowers/

# Local PostgreSQL transfer packages and pre-import safety backups.
Alive数据交换/
*.dump
*.dump.zip
